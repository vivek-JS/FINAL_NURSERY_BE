Place the ICICI Node SDK packages from the bank here (or install from provided .tgz files).

Then either:
- Update package.json "file:" paths to point to those packages, or
- Replace the contents of sdk-distribution/stubs/eazypay (and other @icici stubs) with the real packages.

Copy sdk-api-config.json from the SDK into FINAL_NURSERY_BE/config/sdk-api-config.json (overwrite the template).
